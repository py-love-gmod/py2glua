# Файл переписывает типизацию на более адекватную

# Глобальные
string -> str
number -> int | float
boolean -> bool
any -> Any
function -> Callable
vararg -> Any
# Это несовсем правда, но похуй
table -> list | dict

# Enums
number{CHAN} -> CHAN
number{SNDLVL} -> SNDLVL
number{SND} -> SND
number{FORCE} -> FORCE
number{FCVAR} -> FCVAR
number{RT_SIZE} -> RT_SIZE
number{MATERIAL_RT_DEPTH} -> MATERIAL_RT_DEPTH
number{TEXTUREFLAGS} -> TEXTUREFLAGS
number{CREATERENDERTARGETFLAGS} -> CREATERENDERTARGETFLAGS
number{MAT} -> MAT
number{DMG} -> DMG
number{TRACER} -> TRACER
number{AMMO} -> AMMO

# Struct
table{Problem} -> Problem
table{HTTPRequest} -> HTTPRequest
table{BalloonData} -> BalloonData

# Some fun
table<PhysCollide> -> list[PhysCollide]

# General -///-
table<number> -> list[int | float]
table<string,function> -> dict[str, Callable]

# CNavArea
CNavArea.GetIncomingConnections.return -> Self
CNavArea.GetLadders.return -> Self

